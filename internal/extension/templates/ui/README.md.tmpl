# {{.Name}}

Inari UI extension (`{{.Kind}}`), scaffolded by `inari extension init`.

## Develop

1. `npm install`
2. Register your blueprint slots from `@inari/ui-plugin-sdk` in `index.tsx`.
3. `npm run dev` to run standalone against a dev control plane.

## References

- Platform plan §5.8 (extensibility model)
- `inari-ui-plugin-sdk` for typed extension points (blueprints)
